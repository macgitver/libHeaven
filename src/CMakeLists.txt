
PROJECT( HEAVEN )
INCLUDE_DIRECTORIES( ${HEAVEN_SOURCE_DIR} )

INCLUDE( ../cmake/hic.cmake )

ADD_SUBDIRECTORY( libHeaven )
ADD_SUBDIRECTORY( hic )

IF( HEAVEN_TESTER )
    ADD_SUBDIRECTORY( Tester )
ENDIF()

